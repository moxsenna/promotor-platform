{
  "name": "@promotor/platform-core",
  "version": "0.1.0",
  "description": "Shared platform logic for the Promotor platform: phone normalization (E.164) and formatting. Consumed from source by workspace apps; no emit/build in M0.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@promotor/contracts": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}