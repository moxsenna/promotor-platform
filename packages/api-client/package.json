{
  "name": "@promotor/api-client",
  "version": "0.1.0",
  "description": "Canonical HTTP/API boundary for the Promotor platform (spec §6.3). M0 shell only: config type + typed error; no endpoints until a backend exists. Consumed from source by workspace apps; no emit/build in M0.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}