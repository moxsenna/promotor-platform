// PromotorFlow Fixtures - Deterministic seed data
// Uses same contact_ids as promotor-class-fixtures for integration testing
// ONE PERSON = ONE CONTACT = ONE contact_id

export * from "./seed-contacts";
export * from "./seed-next-actions";
