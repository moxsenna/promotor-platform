{
  "name": "@promotor/contracts",
  "version": "0.1.0",
  "description": "Runtime-validatable shared DTO/contract schemas for the Promotor platform (Zod). Consumed from source by workspace apps; no emit/build in M0.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "files": [
    "src"
  ],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^3.24.1"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}