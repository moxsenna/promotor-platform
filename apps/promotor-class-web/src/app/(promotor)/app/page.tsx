import { ScaffoldNote } from "@/components/scaffold-note";

export default function PromotorHomePage() {
  return (
    <ScaffoldNote
      route="/app"
      title="Promotor workspace"
      note="Scaffold route. Live workspace screens (signals, contacts, programs) land with the mock state layer."
    />
  );
}
