import { ScaffoldNote } from "@/components/scaffold-note";

export default function LearnerHomePage() {
  return (
    <ScaffoldNote
      route="/learn"
      title="Learner experience"
      note="Scaffold route. Live learner screens (program access, lessons, reflection) land with the mock state layer."
    />
  );
}
