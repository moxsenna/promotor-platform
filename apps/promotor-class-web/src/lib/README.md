# lib

App-level helpers, no React components.

Planned per the M0 plan:

- `lib/video/` — exactly three files: `parse-youtube-url.ts`, `youtube-id.ts`, `youtube-embed.ts` (YouTube unlisted + official embed + manual completion; no other video code).
- Route/path helpers and formatters land here as routes get implemented.

Nothing in M0.4 — do not add speculative helpers.
