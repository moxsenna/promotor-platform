/**
 * Learning domain — M0 boundary file.
 *
 * Canonical types live in @promotor/contracts (LearningEventEnvelope,
 * IntegrationEventEnvelope, event payloads from learning-events.ts).
 *
 * M0.6 mock adapter adds lesson completion + reflection submission, which emit
 * learning events, here. Video helpers live in lib/video (parse-youtube-url,
 * youtube-id, youtube-embed only — no other files).
 */
export {};
