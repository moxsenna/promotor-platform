/**
 * Enrollments domain — M0 boundary file.
 *
 * Canonical types live in @promotor/contracts (Enrollment, EnrollmentStatus,
 * EnrollContactInput from enrollments.ts; EnrollmentId from ids.ts).
 *
 * M0.6 mock adapter adds enrollment queries and the acquisition mutation
 * (public registration creates enrollment) here.
 */
export {};
