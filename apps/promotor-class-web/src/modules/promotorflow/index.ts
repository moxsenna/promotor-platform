/**
 * PromotorFlow integration domain — M0 boundary file.
 *
 * Canonical types live in @promotor/contracts (PromotorFlowAdapter,
 * PromotorClassAdapter, FlowContactContext, AssessmentStatus, NextActionRef,
 * LearningActivityProjection from promotorflow.ts).
 *
 * M0 lifecycle: create/request only, show reference, "Open in PromotorFlow".
 * completeNextAction / rescheduleNextAction are forbidden in M0.
 */
export {};
