/**
 * Programs domain — M0 boundary file.
 *
 * Canonical types live in @promotor/contracts (Program, Module, Lesson,
 * ProgramSummary, program ids from programs.ts / ids.ts).
 *
 * M0.6 mock adapter adds program queries (catalog, detail, curriculum) here.
 */
export {};
