/**
 * Contacts domain — M0 boundary file.
 *
 * Canonical types live in @promotor/contracts (Contact, ContactId from identity.ts /
 * ids.ts). Screens import Contact from @promotor/contracts directly. Contact is
 * never redefined in web code.
 *
 * M0.6 mock adapter adds the contact query/mutation port here.
 */
export {};
